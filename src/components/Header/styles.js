import styled from 'styled-components';

export const Container = styled.header`
    padding: 20px;
    background: #C8161D;

    p {
        font-size: 16px;
        color: #FFFFFF;
    }

    span {
        font-size: 12px;
        text-transform: uppercase;
        font-weight: bold;
        color: #FFFFFF;
    }
`;